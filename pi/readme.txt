All software which runs on the Pi

The logic is all hooked together by command_and_control.py, run it with ./run.sh

It is mostly just using empty interfaces from the libraries folders. so please fill these is with what ever.
